# specify path to hostmasternode.conf or leave blank
# default is the same as hostmasternodeCore
energi_conf=/root/.energicore/energi.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
